# Only check files staged for this commit
STAGED=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(ts|tsx|js|jsx)$' || true)
if [ -n "$STAGED" ]; then
  echo "$STAGED" | xargs npx oxfmt --check
  echo "$STAGED" | xargs npx oxlint
fi
